package cp.player.kmp.playback

// Platform media controls are owned by the app layer so platform UI can access the
// shared PlaybackController without coupling the core playback module to UI lifecycles.
