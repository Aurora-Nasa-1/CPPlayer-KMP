- 平台相关能力（文件选择、系统操作）通过 commonMain 中的 expect 接口声明，由 androidMain/desktopMain 以 actual 实现，UI 层仅依赖抽象。
- 每个 Screen 对应一个同名的 ScreenModel（位于 ui/model），用于封装该页面的状态与副作用，避免在 Composable 中直接持有复杂状态。
- 主题定义集中在 ui/theme，并按平台拆分 Theme.android.kt / Theme.desktop.kt，由各自平台的入口加载。