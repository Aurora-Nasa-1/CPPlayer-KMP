Kotlin Multiplatform (Android minSdk 29 / JVM 21)；HTTP 使用 Ktor Client（core + okhttp + JSON 序列化）；播放层在 Android 依赖 Media3 ExoPlayer，Desktop 依赖 JavaFX + dbus-java (MPRIS) + JNA (libmpv/SMTC)；状态管理基于 kotlinx.coroutines Flow + MutableStateFlow。