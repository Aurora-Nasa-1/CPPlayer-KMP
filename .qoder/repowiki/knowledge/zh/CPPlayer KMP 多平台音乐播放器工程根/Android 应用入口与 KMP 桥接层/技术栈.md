Android Application (AGP) + Kotlin Compose + Kotlin 21 编译目标；通过 Gradle 插件别名 `libs.plugins.android.application`、`libs.plugins.kotlin.compose` 管理构建；Git SHA 通过 `ProcessBuilder` 在构建期注入 `BuildConfig.GIT_SHA`。